# TODO

- [x] apps-verify.sh fails
- [x] desktop-verify.sh fails
- [x] all scripts need to be more verbose at every step to be useful in github workflows
- [x] integrate texlive niceties likely from archive repo in github
- [x] also get the brew setup from one my past projects where brew packages were immediately available after 1st login
- [ ] Make sure security best practices are used throughout the halcyon project
- [ ] verify that the python packages installed correctly.
- [ ] gets some just files from my previous projects
- [ ] Check that pyprland systemd service is running or not in the running system
- [ ] try to avoid install steam, lutris from flatpaks
- [ ] From the logs make sure all non-fedora repos are removed after package is installed from it. Also make sure that everything is correct and in order and that the github workflow performed without issues.
- [ ] Carefully study the rakuos linux project and its various repositories and then, from the rakuos project, implement P03 kernel, native-gaming, nvidia integration with P03 kernel, plymouth theming with my own logo while always using the bluebuild system. Do not implement the Containerfile system from the rakuos repos. Also do not implement their rum package manager. And only explain to me in a nix.md file how the rakuos project sets up nix.
- [ ] Convert all flatpaks to manual installs, including mailsping
- [ ] Implement adguard home to halcyon and most abilities from opensense
- [ ] Implement hardened security for all the files in public repo and github workflow, as well as for the github repo.
- [ ] Harden the custom image from securefin linux as well
- [ ] Setup distroshelf as dnf spec and install from rpm
- [ ] Install bitwarden as rpm download from official repos.
- [ ] Install ticktick as rpm from official sources

- Before you start doing anything, determine which version of thinking level should I use for the following prompt, low, high or max?:

There is also the fedora-bootc-minimal. What is the difference between fedora-bootc-minimal, fedora-bootc and ublue-so/base-main and which of the three would be better if I were to switch from bluebuild system to Containerfile with p03-kernel from https://github.com/CatPieLeaf/linux-p03 and its associated nvidia-open. I want to setup everything from scratch with everything put in brick by brick and mimic my current halcyon custom image otherwise. Transition to rpm packages will be done later. I want to install all the flatpak apps under install from flatpak recipe as rpm file. distroshelf is the only one that needs a custom dnf spec to build from. bitwarden, ticktick can be downloaded as rpm from the official sources but these two rpm packages must have a dnf spec similar to how PKGBUILDs in arch linux setup and install bitwarden, ticktick but with automated version management. In, fact all rpm packages must have automated version management. Similarly a PKBUILD like dnfspec must also be setup for obsidian but obsidian needs to be form into an rpm before it can be installed, although the PKGBUILD for obsidian in arch linux official gitlab repo uses a .tar.gz that must be converted to rpm. Similarly all the packages in homebrew list must also be installed as rpm whether the dnf spec and build files for these brew packages are obtained directly from fedora, fedora coprs or terral. But all the rpm packages installed must be automatically the latest version without user intervention in some form of workflow even if the sources for the building the rpms are behind the latest release version. I also need to setup necessary spec and build files to install pyprland as rpm packages. Currently, the texlive packages in the fedora repo are not continuously updated unlike the method in the install-texlive.sh. I need to setup dnf packages similar to how arch linux official repos classifies the differences texlive packages into groups unlike the various incremental rpm packages that fedora provides. But these texlive group packages would be dnf packages but must keep track with the upstream texlive sources. I will update texlive package groups only monthly though or 3 weeks. Like how fyralabs has a monorepo for terra packages, I will keep a monorepo for all my rpm packages including ones that I install from copr repos like the ones for hyprland and zen-browser. Then determine if this monorepo will also contain packages like brave-browser and vscode that been setup in current holcyan repo and be suitable in that repo. Zotero is also in the rpm packages in this mono-repo. Rename the Andaman-migration.md file to an appropriately named markdown file and it should a thorough guide and walkthrough and setup templates and design to do everything I described. Add to the markdown file, a comprehensive, dnf spec templates and other template files needed to successfully build the rpm and install them. When rewriting this markdown file, ask me questions before hand before you decide to go with anything. Assume that I have decided to go the p03-kernel router keeping in mind that selinux is still in plan. The markdown file will contain everything from a-to-z that I described in this prompt. I am moving away from bluebuild to Containerfile system. You must iterate through your plan three times before writing down the final markdown file in Act Mode. Search the web and think longer for these tasks. And make sure you have the latest information till September 2026. You must verify every piece of information you get. And the markdown file must be presented in stages there are lots of information to cover.
