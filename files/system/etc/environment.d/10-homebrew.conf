# Homebrew environment for systemd user sessions, Wayland compositors, and GUI apps.
#
# /etc/profile.d/brew.sh (from the Bazzite base, ublue-os/brew) only fires for
# interactive shells ([[ $- == *i* ]]).  The systemd --user instance, Hyprland,
# Noctalia and apps launched from the desktop inherit a PAM/systemd environment
# that does NOT source profile.d.  This file is picked up by
# systemd-environment-d-generator and injects the brew bin dirs into every user
# session's environment unconditionally — making installed brew packages available
# immediately after first login without requiring a new shell.
#
# Brew bin dirs are appended (not prepended) to preserve system-binary priority,
# consistent with the upstream ublue-os/brew design decision.
#
# IMPORTANT: Do NOT use bare `PATH=$PATH:...` here.
# systemd-environment-d-generator runs without PATH in its environment (clean
# PID-1 context).  `$PATH` therefore expands to the empty string, producing
# `PATH=:/home/linuxbrew/...` — a PATH that starts with `:` and contains no
# system directories (/usr/bin, /usr/sbin, etc.).  That broken PATH is then
# injected into every PAM session via pam_systemd, causing `command not found`
# errors for grep, sed, sudo, readlink and friends inside login shells and GUI
# apps.
#
# The `${PATH:-<fallback>}` syntax is supported by environment.d(5) (systemd ≥
# 233) and makes the standard Fedora system PATH the baseline whenever $PATH is
# absent, so system binaries are always reachable.
PATH=${PATH:-/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin}:/home/linuxbrew/.linuxbrew/bin:/home/linuxbrew/.linuxbrew/sbin
HOMEBREW_PREFIX=/home/linuxbrew/.linuxbrew
HOMEBREW_CELLAR=/home/linuxbrew/.linuxbrew/Cellar
HOMEBREW_REPOSITORY=/home/linuxbrew/.linuxbrew/Homebrew
