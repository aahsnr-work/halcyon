[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "fp"
version = "1.0.0"
description = "Fuzzy file/directory previewer built on fd and fzf."
requires-python = ">=3.10"
license = "Apache-2.0"

[project.scripts]
fp = "fp:main"

[tool.setuptools.packages.find]
where = ["src"]

